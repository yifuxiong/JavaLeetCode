package DFS;

public class Solution {
    // T130(与T200差不多), T200(岛屿的数量), T419(和T200一模一样),
    // T695(岛屿的最大面积), T1034(只跟边界染色)

    // T671(找一个比根节点大的最小值)

    // T863(二叉树向三个方向递归：左子树，右子树，父节点)

    // dfs递归遍历：LCP07传递信息, T1600
}
